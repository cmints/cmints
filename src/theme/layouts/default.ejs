<!DOCTYPE html>
<html lang="en-US">
<head>
  <title>{title[Page title] <%= page.title %>}</title>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="/css/main.css">
</head>
<body>
  <% include ../partials/header %>
<main>
  <div class="col5 device">
    <!-- Navigation Start -->
    <%if (page.showDocNav) { %> 
      <aside id="sitemap">
        <%- include('../partials/navigation', {items: [getSitemap("documentation")]}) %> 
      </aside>
    <% } %>
    <!-- Navigation End -->

    <article class="cell2-4">
      <%- body %>
    </article>

    <!-- TOC Start -->
    <%if (page.showDocNav) { %>
      <aside id="toc">
        <% include ../partials/toc %> 
      </aside>
    <% } %>
    <!-- TOC End -->
  </div>
</main>
  <% include ../partials/footer %>
</body>
</html>
